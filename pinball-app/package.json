{
  "name": "pinball-app",
  "version": "1.0.0",
  "private": true,
  "description": "Solana pinball — Express API and static frontend in one package",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@project-serum/anchor": "^0.26.0",
    "@solana/spl-token": "^0.4.13",
    "@solana/web3.js": "^1.98.0",
    "cors": "^2.8.5",
    "@weirdorg/dotenv": "^1.0.3",
    "express": "^4.21.2",
    "mongoose": "^8.12.1",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
